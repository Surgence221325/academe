import React from "react";
import ReactDOM from "react-dom/client";
import Forum from "./Forum";
import "./forum-components/Home";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Forum />
  </React.StrictMode>
);
